CREATE TABLE CLASS
(
CLASS_ID VARCHAR(100),
SUBJECT VARCHAR(100), 
CATALOG_NUMBER VARCHAR(100), 
SECTION_NUMBER VARCHAR(100), 
TITLE VARCHAR(100), 
UNITS VARCHAR(100), 
TYPE VARCHAR(100), 
DAYS VARCHAR(100), 
TIME VARCHAR(100), 
BUILDING VARCHAR(100), 
INSTRUCTOR VARCHAR(100),
UPDATED TIMESTAMP DEFAULT NOW() ON UPDATE NOW(),
PRIMARY KEY (CLASS_ID)
)ENGINE = MyISAM;

CREATE TABLE CLASS_ID
(
ID VARCHAR(100) NOT NULL,
ADDED TIMESTAMP,
PRIMARY KEY (ID)
)ENGINE = MyISAM;

CREATE TABLE USER
(
UID INT UNSIGNED NOT NULL AUTO_INCREMENT,
NAME VARCHAR(100),
PASSWORD VARCHAR(100),
PASSWORD_SALT VARCHAR(50),
AUTH_KEY VARCHAR(100),
EMAIL VARCHAR(100),
AVAILABILITY VARCHAR(500),
UPDATED TIMESTAMP DEFAULT NOW() ON UPDATE NOW(),
PRIMARY KEY (UID,NAME)
)ENGINE = MyISAM;

CREATE TABLE USER_AVAILABILITY
(
USER_ID INT NOT NULL,
TIME INT NOT NULL,
PRIMARY KEY (USER_ID,TIME)
)ENGINE = MyISAM;

// Can't create on my server
CREATE TRIGGER STUDENT_UPDATE BEFORE INSERT ON STUDENT
FOR EACH ROW BEGIN NEW.UPDATED = now();

CREATE TABLE COLLEGE
(
NAME VARCHAR(100),
ID VARCHAR(100) NOT NULL,
ADDED TIMESTAMP,
PRIMARY KEY (ID)
)ENGINE = MyISAM;

CREATE TABLE GROUPS
(
GID INT UNSIGNED NOT NULL AUTO_INCREMENT,
NAME VARCHAR(100),
AVAILABILITY VARCHAR(500),
UPDATED TIMESTAMP DEFAULT NOW() ON UPDATE NOW(),
PRIMARY KEY (GID,NAME)
)ENGINE = MyISAM;

CREATE TABLE USER_GROUP
(
USER_ID INT NOT NULL,
GROUP_ID INT NOT NULL,
PRIMARY KEY(USER_ID,GROUP_ID)
)ENGINE = MyISAM;